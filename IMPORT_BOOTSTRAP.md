# One-off history import bootstrap

This branch exists only to trigger and verify the history-preserving copy from `blakinio/Oteryn-v2` into `Oteryn/Oteryn-Game`.

It must not be merged. The import workflow removes this branch after successful verification.
