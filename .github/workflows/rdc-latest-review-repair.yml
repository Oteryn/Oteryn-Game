name: RDC latest review repair

on:
  push:
    branches:
      - governance/remote-desktop-per-action-gate-237

jobs:
  repair:
    runs-on: ubuntu-24.04
    steps:
      - name: Confirm helper execution
        run: echo helper-ok
