# Neutral documentation routing canary

Temporary CI canary used only to verify that an isolated neutral Markdown change selects `rust=false` and `windows=false` after the audited document-consumer baseline refresh.

This file carries no product, runtime, architecture, contract, governance, allocation, migration, or implementation authority and is not intended for protected integration.
