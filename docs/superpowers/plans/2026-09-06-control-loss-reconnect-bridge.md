# Complete owning-loss reconnect bridge353 implementation plan

## Authority and scope

Work162/Issue353 implements the concrete caller bridge excluded by protected348, under existing accepted FND-04B/recovery/protection and resource contracts. Foundation343 is protected atedef416745f92b79371f98739272c840b0a9b357 and remains completed; original338 history is retained. Current allocation source53c6bdf06a2282d893035a995c46052c88f935b4. No writer is admitted until this exact allocation and338 release are protected and Work binds readback.

Own only `apps/game-server/src/foundation/admission_recovery_inner.rs`, new `apps/game-server/src/foundation/control_loss_reconnect_bridge_tests.rs`, own353 task and this plan. No B/SQL/Cargo/lib/facade/verifier/registry/workflow/source/other repository edits. Preserve existing V1/V2 public representations and wire semantics; concrete additional paths require protected amendment. One sole semantic writer, path-disjoint from B and351, explicit Work third-lane justification5560287018; no two Foundation writers.

## 1. Trace and reproduce the integration boundary

Read accepted FND-04B §§5/12, protected owning-loss addendum, complete control-loss/recovery types, legacy reconnect prepare/final/flow and actual B codec/SQL/current-session consumers read-only. Inventory every complete protection/rearm/epoch/budget field and generation namespace. Reproduce why legacy Unused grants protection and invented Fenced creates history; a receipt retaining bytes does not repair an unrepresentable mandatory PREPARE field. Use real independently sealed positive controls and exact counterexamples, not a no-op/mismatched-source negative.

## 2. Implement the additive complete envelope

Design an independently versioned internal durability continuity/operation, not a new network protocol, preserving original loss identity and all complete retained state. Existing verified V1/V2 credential binding remains consumed normally. Keep legacy APIs/enums unchanged; use distinct additive predicates/flow where no faithful existing representation exists. Do not downcast NotEntitled or re-label entitlement/rearm generation as connection generation.

Separate immutable historical operation from live current capability. PREPARE and final decisions re-resolve independently current owning source/session/controller absence, actor/account/world/lease/runtime and claims under supplied trusted time/fences. Bind exact original grace/loss epoch, current connection generation and candidate transport. New current facts must not be derived from the same stored record merely to make equality pass. Missing source or unknown version fails closed. Preserve accepted original-deadline/budget and retry behavior, including all eight retained attempts.

NotEntitled produces no protection. Unused eligibility, prior activation/deadline/consumption and rearm data follow existing accepted full-continuity rules; select no new duration or namespace equivalence. Sealed effects remain inert until B's matching atomic transaction. Completion, historical exact replay, reconciliation and current adoption must remain distinct; an old receipt cannot authorize a new effect or reinstall control. Preserve original L on retry/restart, full operation matching, and stale/current-supersession denial. Use accepted operation/resource bounds before new comparisons/copies; do not truncate/hash away provenance or claim complete B/driver accounting from local field guards.

## 3. Qualify the complete semantic boundary

Cover NotEntitled, Unused and Activated histories with meaningful positives and independent negatives across applicable V1/V2 paths. Mutate original loss/epoch/grace, entitlement/rearm generation, consumed/deadline state, retained attempt, candidate generation/transport, current owner/actor/claims, trusted time and source availability separately. Check prepare/final, exact/conflicting retry, lost response/restart, completion/reconciliation and current adoption. Keep existing legacy and post-grace regressions unchanged and compile-fail anti-forgery evidence.

Run full current game-server package/doctests, strict all-target Clippy, fmt/whitespace/governance, and compile actual canonical B source-included durability target with the candidate in a separate qualification cache. Never edit B's writable tree or count unconfigured SQL as actual execution. Review all owned changes plus affected existing contracts/consumers; require independent non-author exact-material review, exact-head canonical CI and normal Merge Queue. Persist material findings and counters across windows; no no-op retrigger or unsafe legacy compatibility shortcut.

## 4. Protected handoff and consumer boundary

Work verifies protected integration and releases this semantic lease. Only then does existing B consume the complete envelope through its separately owned version-aware codec/SQL/current-source path and prove actual PostgreSQL17.6 recovery/race/restart. A later post-grace SQL successor remains separately scoped. Actual producer registration/ChildC and full B/driver acceptance still gate247; semantic bridge completion alone cannot release Server Seam.
