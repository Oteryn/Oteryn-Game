# Native admission resource registry #342 implementation plan

Prospective exact allocation; no writer before its protected integration/readback. Source339, resource341 and the explicit #342 configuration addendum must be accepted. Actual allocation merge is immutable admission; Work grants one bounded exclusive writer.

## Exact scope

Only docs/contracts/RESOURCE_LIMITS_REGISTRY.json plus one coordinator-allocated task and one plan. Preserve 67 existing entry objects unchanged. No validator/runtime/schema/Cargo/source/Platform/bootstrap edits.

## Execution

1. Refresh main, both decisions' actual protected merge evidence, root/docs instructions, issue/branch/path ownership and registry hash. Preserve immutable admission separately from integration base.
2. Verify the protected #342 configuration addendum, then register each source/assignment cap with minimum=maximum at its existing accepted maximum. This constrains configuration, not actual use. No smaller profiles are supported. Exact 32-byte/43-character assignment identity retains fixed semantic width. DFR ranges remain explicitly fixed.
3. Independently enumerate and map all accepted subdimensions; the preliminary 87-row scaffold is not authority. For each row, verify units, arithmetic, precise per-slot/process scope and failure classifications against source paragraphs. Internal resource errors do not add a wire category. Structural failures remain invalid input; timeout/dependency unavailable; submitted timeout remains ambiguous.
4. Preserve inherited 64 pending / 256 domain maxima together; eight attempts can have 512 physical children. Returned rows differ from physical rows and logical roots differ from required physical locks. Full cross-epoch predicate remains intact.
5. Preserve end-to-end slot and restart custody; all copies charged, timeout never grants new capacity, permanent history never evicted. Source TLS limits require real enforceability later. No runtime or capacity claim from this registration.
6. Validate JSON/unique IDs/required nonnull fields/ranges, compare all previous entry objects unchanged, check each decision dimension has exactly one applicable mapping or explicit inherited reference. Run existing governance and selected CI. Independent whole-diff semantic mapping review, protected merge queue and readback.

## Verification nuance

Max and max+1 tests are future implementation obligations, not tests executed by these docs. Exact-width identity also requires max-1 rejection. Time boundaries need just-before/at/after monotonic deadline and ambiguous submission tests. Byte caps use largest representable canonical value at/below cap and first above plus exact generic length gate. Client-visible=false means no new direct protocol field/category; availability impact on admission/recovery still exists.

## Separate B amendment

Coordinator must explicitly allocate apps/game-server/src/bin/oteryn-game-migrate.rs to B before any edit. Minimal intended composition change removes source-included Durability and imports the public library MigrationExecutor; no semantics, public seal visibility, Cargo or Foundation change. This draft does not write or allocate that path.
