# Tibia forum evidence — continuation, 2026-09-12

Read [the current synthesis of 720 unique thread IDs](./synteza-720.md). This is a partial research checkpoint, not a complete forum archive or authorization to implement gameplay changes. The [708-thread synthesis](./synteza-708.md) and [636-thread synthesis](./synteza-636.md) remain historical snapshots.

Retained: 600 historical records plus 120 additional Last Year records and metadata for 241 observed posts. This continuation adds 84 threads and 174 posts to the preceding 636-record checkpoint. One newly checked thread contains only deleted source text and is explicitly marked source_removed. Full third-party post text is not retained here.

The [combined index](./source-index.json) resolves all 720 entries to retained analysis rows and preserves historical records. Post IDs and hashes describe sampled coverage, not all replies. Different handles do not prove different people, and bumps do not prove support. Linked images were not analyzed. The [quality index](./year-quality-index.json) covers the latest 84 records, not the entire older corpus. Twenty-five are excluded from problem recurrence; many remain valid proposal candidates. Two newly verified duplicate pairs preserve both IDs but count one OP text each.

Observed Last Year list: 3061 threads / 103 pages. All 103 list pages were inventoried; 628 IDs match retained records and 2433 remain without retained analysis. The rolling list grew from the requested 102 pages. [Progress and resume queue](./last-year-progress.json) preserve page membership and pending IDs; the count match is not proof of an atomic snapshot. All Threads and Last Year pagination are not interchangeable. No background collection or schedule is running.

Comparison revision: PR #571 at b2ef322791520130a2683f3004d26abd5c027ac8. Sol medium drafted the bounded analyses; the lead checked observed post IDs, totals, source-row pointers, material classifications, official Market/interface documentation and specific roadmap mappings. Current runtime mechanics not tested remain unknown. Historical July balance reports must not be presented as current Global values.

Validation: 720 unique IDs, no overlap between 84 newly imported and 636 preceding records, 174 newly sampled post IDs and publication/edit timestamps checked against observed inputs, and all 720 source-row pointers checked. Quality review corrected both over-rejection of short complaints and unsupported roadmap mappings. Repository governance validation and whitespace checks are run for this import.
