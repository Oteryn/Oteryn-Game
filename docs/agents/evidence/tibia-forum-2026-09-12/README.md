# Tibia forum evidence — continuation, 2026-09-12

Read [the live progress and research registers](./progress-current.md). The complete target is all 3061 captured Last Year IDs; collection and analysis are unfinished. Inventory coverage must not be presented as content coverage.

The [source index](./source-index.json) records stable thread IDs and exact retained analysis rows. Newer entries include post IDs, publication/edit timestamps and hashes of the observed capture. Coverage is OP and up to three initial replies, not full discussions. Author-removed source text is marked explicitly. Full third-party post text and profile details are not published here.

The [resume queue](./last-year-progress.json) preserves the rolling 103-page inventory. All Threads and Last Year pagination are not interchangeable. Historical synthesis snapshots retain their original counts; the current report takes counts from the indexes.

The [quality index](./year-quality-index.json) separates concrete needs, proposals, duplicates, mixed discussions, missing text and unsupported claims. A poor proposal can reveal a valid need. Shortness, spelling, emotion or disagreement alone do not justify rejection. Post count, view count and repeated replies do not establish independent support.

Comparison revision: PR #571 at b2ef322791520130a2683f3004d26abd5c027ac8. This evidence authorizes no gameplay implementation or merge. Bounded Sol medium analyses are checked against observed source content and the proposal roadmap. Global runtime behavior remains unknown where it has not been independently verified.

Validation for imports: unique thread IDs, observed sampled post IDs and dates, source-row pointers, inventory matched/pending partition, repository governance and whitespace checks. There is no scheduler or autonomous collector running after the active task ends.
