# OTV2-20260806-identifier-owner-baseline

```yaml
task_id: OTV2-20260806-identifier-owner-baseline
title: Record owner-accepted identifier, instance and social-presence baselines
mode: CONTRACT
status: ready
repository: blakinio/Oteryn-v2
base_branch: main
branch: docs/fnd-id-01-owner-baseline
pr: 56
base_sha: 26b5fa275fba19fdee0e26a6f65263489af3e500
architecture_head_reviewed: 80048871a06dae381c8bcec9e1c8978607236cb8
owner: ChatGPT architecture coordinator
created_at: 2026-08-06T14:19:00+02:00
updated_at: 2026-08-06T20:28:00+02:00
owned_paths:
  - docs/architecture/FND-ID-01_OWNER_ACCEPTED_BASELINE.md
  - docs/architecture/UUIDV7_DURABLE_IDENTITY_OWNER_BASELINE.md
  - docs/architecture/INSTANCE_SCOPE_AND_RUNTIME_OWNER_BASELINE.md
  - docs/architecture/SOCIAL_PRESENCE_AND_CONTACT_CONSENT_OWNER_BASELINE.md
  - docs/agents/tasks/active/OTV2-20260806-identifier-owner-baseline.md
public_contracts:
  - docs/architecture/FND-ID-01_OWNER_ACCEPTED_BASELINE.md
  - docs/architecture/UUIDV7_DURABLE_IDENTITY_OWNER_BASELINE.md
  - docs/architecture/INSTANCE_SCOPE_AND_RUNTIME_OWNER_BASELINE.md
  - docs/architecture/SOCIAL_PRESENCE_AND_CONTACT_CONSENT_OWNER_BASELINE.md
depends_on:
  - ADR-0001 through ADR-0011
  - FND-01 and VSL-02 destination cutover
blocks: []
cross_repository_coordination_id: OTV2-NATIVE-FOUNDATION
external_repositories:
  - blakinio/otclient
```

## Outcome

Persist the product owner's accepted identifier, UUIDv7, channel, world-scoped party, instance-runtime, map-template, activity-admission, seamless-handoff and privacy-first social baselines as canonical architecture input without claiming that the complete `FND-ID-01`, `FND-02`, `FND-03`, `FND-04`, durability, Party Finder or instance-lifecycle contracts are complete.

## Delivered owner decisions

### Durable identity representation

- Every independently addressable semantic entity owned by Oteryn-v2 that crosses a process, service, GameNode, protocol, persistence, durable-event, audit, migration, backup or recovery boundary defaults to a strongly typed UUIDv7.
- Cross-repository and externally owned identifiers adopt UUIDv7 only through their authoritative contract or coordinated migration.
- UUIDv7 identifies a durable entity; it is not a bearer secret, capability, ordering value or proof of current authority.
- Raw UUID values are not interchangeable in domain APIs; separate strong types and required scope wrappers remain mandatory.
- Nil/zero UUID is not a valid entity identity or magic sentinel; absence is explicit.
- Durable IDs are immutable, non-reused and generated by the authoritative domain owner without a central global UUID service.
- Collision or duplicate insertion cannot overwrite an existing entity and must fail or retry with audit evidence.
- UUIDv7 timestamp order is never used as gameplay causality, writer authority or exact command order.

### Aggregate, runtime, protocol, ordering and content separation

- Serialization or GameNode transfer does not automatically make every nested runtime object an independent durable entity.
- Creatures, timers, effects and other nested state may cross boundaries inside a fenced aggregate snapshot using snapshot-local references when they have no separate durable lifecycle.
- Hot simulation state uses runtime-local generational handles rather than UUIDv7 as its primary addressing mechanism.
- Frequent movement, visibility, combat and delta traffic uses compact session-local handles after authoritative identity establishment.
- Simulation ticks, sequences, revisions, generations, fencing tokens and offsets are scoped `u64`-class ordering values, not UUID identities.
- Static content uses stable content keys, content revisions and compact bundle/runtime identifiers.
- Tiles, coordinates, chunks, pathfinding nodes, transient effects and per-tick work do not receive UUIDv7 automatically.
- PostgreSQL native `uuid` and canonical 16-byte protocol preservation are accepted architectural baselines for adopted UUID identities; exact schemas, indexes, codecs and handle widths remain later-contract work.
- UUIDv7 creation-time leakage is treated as a privacy concern; internal UUIDs are not automatically public references.
- Capacity planning must benchmark the accepted hybrid: durable UUIDv7 plus aggregate-local/runtime/session/content handles and numeric sequencing.

### Identifier and topology

- `WorldId` globally identifies one logical world.
- Canonical semantic identities are `WorldId + ChannelId`, `WorldId + InstanceId` and `WorldId + PartyId`.
- A globally unique UUIDv7 representation does not erase semantic scope.
- Channels remain the primary persistent-world topology.
- Instances are optional isolated gameplay contexts and do not create another world, economy or character namespace.
- Parties may organize members across channels of one world, while open-world simulation remains channel-local and instanced simulation remains instance-local.

### Instance runtime, map and admission

- Eligible players from several channels of one world may enter one concrete instance.
- One authoritative `InstanceRuntime` owns all admitted participants and instance-local simulation.
- Each participant retains validated origin-channel and return metadata.
- Entry and exit are explicit generation-fenced, idempotent ownership transitions.
- Channel map, revisioned activity-map template and instance-local mutable overlay are separate concepts.
- Immutable geometry, collision and static assets may be shared while players, creatures, doors, effects, timers, objectives, corpses and reward state remain isolated per instance.
- Positions are scoped by `ChannelSpace` or `InstanceSpace`; raw coordinates alone do not identify a location across runtime boundaries.
- Physical levers, Party Finder, quests, events and queues consume one shared authoritative activity-instance engine.
- A fixed five-player boss lever validates the complete group and defaults to all-or-nothing admission.
- Party Finder may admit same-world players directly from several channels into one common instance without an intermediate common channel.

### Seamless transition, completion and return

- Instance entry does not repeat account authentication or character selection.
- Cross-GameNode movement uses make-before-break handoff: reserve destination, prepare background destination context, fence and commit ownership, send a full authoritative snapshot, then retire the source path.
- Same-GameNode connection reuse is only an optimization with identical safety semantics.
- Exact fresh-session, continuation or admission-grant representation remains owned by `FND-04`.
- ADR-0003 remains authoritative: Game Gateway stays in the control plane and does not become a permanent gameplay proxy.
- Rewards, lockouts and inventory mutations are settled authoritatively and idempotently before cleanup.
- Players normally return through validated origin-channel routing to a safe configured exit anchor.
- Unavailable origin routing requires an explicit recovery policy and never silently chooses an arbitrary channel.

### Social privacy

- Exact channel, instance, GameNode and map placement are non-public.
- Contact/VIP creation requires invitation and explicit acceptance.
- Character contact is the default relationship scope.
- Account-wide friendship is separate and requires distinct mutual consent.
- Alternate characters remain hidden by default and require deliberate owner-controlled sharing.

## Acceptance criteria

- [x] Record the four identifier classes and cross-cutting invariants.
- [x] Record accepted world-, channel-, instance- and party-scoped identity semantics.
- [x] Accept strongly typed UUIDv7 for independently addressable Oteryn-owned durable cross-boundary identity.
- [x] Preserve externally owned identifiers until coordinated migration.
- [x] Preserve ADR-0003 ownership of Platform Identity, World Registry and Gateway boundaries.
- [x] Separate durable identity from authorization and fencing.
- [x] Define aggregate/snapshot-local state so migration does not require UUIDv7 for every nested runtime object.
- [x] Require runtime-local generational handles for hot simulation state.
- [x] Require compact session-local handles for frequent gameplay references.
- [x] Keep ticks, sequences, revisions, generations and fencing as scoped numeric values.
- [x] Keep static content and map hot paths revision-scoped and compact.
- [x] Record native PostgreSQL UUID, full 128-bit wire preservation, privacy and capacity constraints without freezing implementation details.
- [x] Record channels as primary topology and instances as isolated optional contexts.
- [x] Permit same-world cross-channel admission into one concrete instance.
- [x] Require one authoritative `InstanceRuntime` and prohibit dual writers.
- [x] Record origin routing and fenced entry/exit ownership transitions.
- [x] Separate channel map, activity-map template and instance-local mutable overlay.
- [x] Record channel/instance-scoped spatial identity.
- [x] Define one shared activity-admission engine for physical triggers and Party Finder.
- [x] Define fixed-group all-or-nothing admission and direct cross-channel Party Finder entry.
- [x] Record seamless no-relogin make-before-break handoff while preserving ADR-0003.
- [x] Record authoritative completion, idempotent settlement, return and cleanup ordering.
- [x] Preserve privacy-first presence, contact and alternate-character decisions.
- [x] Reconcile accepted decisions through an explicit supersession rule rather than silently rewriting history.
- [x] Keep the PR documentation-only and limited to five declared Markdown paths.
- [x] Complete adversarial architecture audit of the UUIDv7 semantic delta with zero open material findings.
- [ ] Require all repository checks to pass on the exact unchanged final head before squash merge.

## Excluded scope

- no Rust implementation, runtime, map types or protocol codec;
- no PostgreSQL schema or migration;
- no concrete UUID library or Rust wrapper implementation;
- no exact runtime/session/snapshot-local handle width or bit allocation;
- no concrete Game Session, continuation or admission-grant format;
- no Party Finder implementation or matching algorithm;
- no final capacity, placement, migration, reconnect, reward, lockout or spectator policy;
- no client transition UI implementation;
- no external-repository write;
- no claim that complete foundation, durability or gameplay contracts are finished.

## Validation

### Changed scope

- base: `26b5fa275fba19fdee0e26a6f65263489af3e500`;
- architecture head reviewed: `80048871a06dae381c8bcec9e1c8978607236cb8`;
- changed files: exactly five declared Markdown files;
- runtime/component validation: `NOT_APPLICABLE` because this PR contains architecture documentation only.

### E2E

- result: `NOT_APPLICABLE`;
- reason: no executable client, server, protocol, persistence or deployment behavior is changed; the new decision establishes requirements for later benchmark and contract work.

### Exact-head CI

- required protected-branch status from live ruleset: `Agent governance / validate`;
- all previous exact-head evidence is invalidated by the UUIDv7 semantic commits;
- this audit-closeout commit intentionally creates a fresh `pull_request.synchronize` generation without changing public architecture semantics;
- final result must be `PASS` on the exact unchanged head before merge.

## Independent architecture audit

Architecture head reviewed: `80048871a06dae381c8bcec9e1c8978607236cb8`.

Method: adversarial review against root governance, ADR-0003, the existing FND-ID-01 semantic baseline, multichannel/instance ownership, hot-path capacity, database/wire boundaries, privacy, recovery snapshots and all deliberately unresolved items.

Resolved material findings:

1. **Cross-repository authority ambiguity** — the first UUIDv7 draft could be read as assigning `WorldId` and Game Session generator ownership to Oteryn-v2 despite ADR-0003. Resolved by requiring the authoritative Platform/World Registry/FND-04 contract or coordinated migration before those identities adopt UUIDv7.
2. **Snapshot over-identification** — the first rule could be read as requiring UUIDv7 for every transient object transferred in an instance snapshot. Resolved by defining independently addressable identity versus nested aggregate state and allowing closed, generation-fenced snapshot-local references.

Verified invariants:

- semantic scopes `WorldId + ChannelId`, `WorldId + InstanceId` and `WorldId + PartyId` remain intact;
- common 128-bit representation does not make strong types interchangeable;
- UUIDv7 is not authorization, sequencing or fencing;
- no central generator or global runtime lookup is introduced;
- hot simulation, frequent protocol traffic, content and nested snapshots retain compact handles;
- externally owned IDs are not silently re-keyed;
- clock regression and timestamp leakage are explicitly bounded;
- PostgreSQL and wire guidance remains architectural and does not freeze exact implementation;
- no runtime, protocol, schema or migration implementation is authorized;
- all remaining open decisions are named.

Open material findings: none.

Verdict: `PASS_ZERO_MATERIAL_FINDINGS`.

Delta audit required for the audit-closeout commit:

- expected scope: this task record only;
- public contract semantics changed: none;
- architecture decision files changed after reviewed head: none;
- verdict: `PASS_ZERO_MATERIAL_FINDINGS` if the final comparison confirms task-record-only delta.

## PR and closeout

- PR: #56;
- current state: open;
- review comments and inline threads: none before final exact-head review;
- merge method: squash only after exact-head governance PASS;
- post-merge requirement: archive this task and release the owned paths.

## Context checkpoint

```yaml
last_progress: Completed adversarial audit of the accepted UUIDv7 durable-identity model after resolving Platform authority and aggregate-snapshot boundary findings.
status: ready
branch: docs/fnd-id-01-owner-baseline
architecture_head_reviewed: 80048871a06dae381c8bcec9e1c8978607236cb8
pr: 56
ci_check_generation: pending for audit-closeout head
ci_checks_for_current_head: unknown
terminal_ci_wait_started_at: 2026-08-06T20:28:00+02:00
unchanged_state_checks: 0
identical_failure_retries: 0
repair_cycles_for_current_gate: 2
stall_warnings: 0
blocker: null
next_action: Confirm task-record-only final delta, submit exact-head audit review, obtain Agent governance PASS, then squash merge and archive the task.
```
