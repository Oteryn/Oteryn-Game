# Oteryn #176 branch disposition

This marker exists only to give the duplicate stale prompt-policy ref a current exact closed-PR lifecycle identity. Before this marker it pointed to the same superseded fixed-worker-window proposal as `governance/remove-hourly-worker-stop-2`; current META-bound policy is authoritative.

Do not merge this marker. Closed-PR/Git history preserves the old proposal after deletion.
