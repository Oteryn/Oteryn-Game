# OTV2-20260826-durability-shared-cargo-ci-lease

```yaml
task_id: OTV2-20260826-durability-shared-cargo-ci-lease
title: Integrate Durability shared Cargo and PostgreSQL CI surfaces
mode: COORDINATE
status: completed_released
repository: Oteryn/Oteryn-Game
base_branch: main
allocation_branch: coord/durability-shared-lease-allocation
branch: coord/durability-shared-cargo-ci
issue: 162
blocked_issue: 167
pr: null
base_sha: cb9c5f4f53dd880c9d338dafd21b6184a4419993
head_sha: null
final_head_sha: null
owner: Oteryn: work coordinator
created_at: 2026-08-26T09:20:00+02:00
updated_at: 2026-08-26T09:20:00+02:00
owned_paths:
  - Cargo.toml
  - Cargo.lock
  - apps/game-server/Cargo.toml
  - .github/workflows/rust.yml
  - .gitattributes
public_contracts: []
depends_on:
  - issue:162
  - issue:167 allocation merge
  - docs/architecture/reviews/OTERYN_GAME_DURABILITY_TOPOLOGY_DECISION_PACKET_2026-08-24.md
blocks:
  - issue:167 resume from WAITING_EXTERNAL
  - Server Seam re-evaluation after Durability delivery
external_repositories: []
allocation_pr: 181
allocation_merge_sha: 2e8c1605535167e91fe0c7775e520f599e3d89ea
delivery_pr: 182
delivery_final_head_sha: 0017cac33fef8c7359bdb9f2ba2c6c367ba06495
delivery_merge_sha: 475288b29cadccb73e08eb488160169d296c7874
ownership_release: true
independent_review: PASS
independent_review_packet_sha256: 69dca25b19bcedc48f650dcba96f901d098d64bfbcc6601203a77dfd2acf6c2f
independent_review_response_sha256: 1b8cd28726c5a4f9a8b37b77da9a9b13d3e35a21c9446c88e466d2e17f0305fd
```

## Purpose

Acquire the serialized coordinator lease required after the Durability worker discovered a legitimate shared-path dependency. This task does not change Durability semantics. It integrates only the already accepted SQLx 0.9.0 dependency shape, Cargo 1.94 lockfile, deterministic SQL migration line endings, and an isolated PostgreSQL CI harness required by the canonical Durability topology.

## Proven blocker

- The accepted Durability implementation requires SQLx `=0.9.0` with the frozen feature set from the topology decision packet.
- Current protected `main@cb9c5f4f53dd880c9d338dafd21b6184a4419993` does not expose SQLx from the root workspace or `oteryn-game-server` manifest.
- `Cargo.lock` must be regenerated by canonical Cargo 1.94; it must not be hand-authored.
- Real isolated PostgreSQL DB-E2E requires coordinator-owned workflow support.
- The Durability worker has no authority over these shared surfaces and therefore correctly returned `WAITING_EXTERNAL`.
- This is routine integration under accepted architecture, not `ARCHITECTURE_ESCALATION_REQUIRED`.

## Exact integration scope after this allocation merges

1. Add the accepted workspace SQLx dependency exactly as recorded by the Durability topology:
   - version `=0.9.0`;
   - `default-features = false`;
   - features `runtime-tokio`, `tls-rustls-ring-webpki`, `postgres`, `migrate`, `macros`, `uuid`.
2. Make `apps/game-server` consume `sqlx.workspace = true` and set `default-run = "oteryn-game-server"` for the accepted dedicated migration binary topology.
3. Regenerate `Cargo.lock` using Cargo 1.94 and verify `cargo +1.94.0 metadata --locked`.
4. Add `*.sql text eol=lf` to `.gitattributes` for deterministic migration checksums.
5. Add an isolated PostgreSQL CI service/job that validates locked metadata and runs `apps/game-server/tests/durability_postgres.rs` when that allocated test exists.

## Explicitly excluded

- `apps/game-server/src/lib.rs` composition in this lease; it remains separately serialized until the Durability module exists and can compile.
- any Durability implementation path owned by Issue #167;
- architecture, persistence semantics, resource limits, registry/stable IDs, production database/config/secrets, deployment, Platform, Atlas, META, or external-repository changes;
- any reopening or modification of terminal Issues #115 or #131.

## Required validation

- `cargo +1.94.0 metadata --locked --format-version 1`
- `cargo +1.94.0 check --locked -p oteryn-game-server`
- `cargo +1.94.0 tree --locked -p oteryn-game-server --edges normal,build` proves `sqlx v0.9.0`
- pinned PostgreSQL service reaches healthy state
- workflow YAML parses
- `python tools/agents/validate_governance.py`
- `python tools/repository/validate_repository_policy.py`, with any pre-existing baseline failure explicitly distinguished from regressions
- `git diff --check`
- exact-head GitHub CI and genuinely independent exact-head review before merge of the shared integration PR

## Resume condition

Issue #167 remains `WAITING_EXTERNAL` until this shared integration is merged to protected `main`, the merge is read back, the serialized lease is released, and the Durability worker is given the resulting exact base. Server Seam remains `WAITING_DEPENDENCY` until the actual Durability adapter itself later merges.

## Context checkpoint

```yaml
last_progress: exact shared-path requirement proven; coordinator lease allocation prepared
status: completed_released
branch: coord/durability-shared-cargo-ci
head_sha: null
pr: null
blocker: null
next_action: none_terminal
```

## Terminal reconciliation

The serialized coordinator lease completed through the recorded delivery PR, protected-main readback succeeded, and all listed write authority is released.
