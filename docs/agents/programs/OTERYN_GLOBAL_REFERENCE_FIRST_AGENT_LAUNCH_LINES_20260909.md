# Global Reference-first agent launch lines

- Programme: #486
- Plan: `OTERYN_GLOBAL_REFERENCE_FIRST_PARALLEL_GAMEPLAY_PROGRAMME_20260909.md`
- Purpose: owner/operator copy-paste launch packet
- Runtime/write authority granted by this file: **NONE**

## How to use this packet

Run the **coordinator line once** in the already active #162 control-plane lineage. Do not start a second mutating `work coordinator` or `terra game coordinator` for the same programme.

The specialist lines may run in separate chats/Work tasks. Every direct implementation alias already requires a live exact allocation before it may write. If the allocation is absent, the specialist remains read-only and returns readiness/evidence/allocation-gap output instead of creating its own branch or scope.

All lines inherit these Reference invariants:

- first external target is the immutable post-2026-07-28 Global Tibia production-observable behavior cut;
- official/primary evidence first; OTS repositories are hypothesis/test-discovery only;
- `UNKNOWN`/`CONFLICT` is never filled by convenience;
- `Oteryn Reference` implements evidenced Global behavior in the native Oteryn stack;
- `Oteryn Evolved` changes are excluded until a later explicit release after the Reference readiness checkpoint;
- GitHub LIVE state is the lifecycle source of truth;
- resume existing canonical workers instead of creating replacements.

## 0. Programme coordinator — run once

```text
Oteryn: work coordinator — consume the Global Reference-first programme #486 and `docs/agents/programs/OTERYN_GLOBAL_REFERENCE_FIRST_PARALLEL_GAMEPLAY_PROGRAMME_20260909.md`. Reconcile LIVE #483, #54/#433, #480, #247, #319, #364 and every existing canonical worker before allocating anything. Classify R0-R10 as ACTIVE_EXISTING / READY_PATH_DISJOINT / READ_ONLY_PREP / WAITING_DEPENDENCY / ARCHITECTURE_REQUIRED, continue existing lineages first, and allocate only the smallest path-disjoint children whose prerequisites are already sufficient. Keep production Movement/Combat composition behind their real dependencies, but do not let those blockers freeze independent Reference evidence/content/world/non-production client work. Enforce GLOBAL_REFERENCE_TARGET first, OTERYN_REFERENCE composition second, and no OTERYN_EVOLVED gameplay until a reviewed Reference readiness checkpoint plus later explicit owner/control-plane release. Continue autonomously through legal review/CI/Merge Queue/readback checkpoints; do not create a second control plane or duplicate workers.
```

## R0. Global Reference evidence / parity

```text
Oteryn: architektura — act as the bounded Global Reference evidence worker for programme #486, centered on existing #483 rather than duplicating it. Fresh-read the accepted first Reference baseline and evidence manifest, then build/extend the official-first evidence and gap matrix for world/movement, combat, spells, creatures, items/progression, NPC/services and the minimum Reference Playable journey. Treat current post-target Global evidence only as continuity evidence unless the target behavior is proven unchanged. Canary/Crystal/other OTS sources are `OTS_HYPOTHESIS_ONLY`. Do not invent mechanics, do not introduce Evolved design and do not mutate runtime; return exact `PROVEN / OBSERVED / DERIVED / UNKNOWN / CONFLICT` cases and the smallest next evidence actions.
```

## R1. World / Map

```text
Oteryn: impl content — programme #486 WORLD-MAP lane. Freshly reconcile #54/#433, design checkpoint #64, current `apps/game-server/src/content/**`, the accepted Reference target and active ownership. Prepare or execute only the exact current allocation for Reference world/map semantics: OTBM as migration input, canonical cells/floors/ordered stack, terrain/collision/navigation, roofs/occlusion/elevation, teleport/door/static topology, spawn-area topology, server/client-safe projections and deterministic no-silent-loss diagnostics. Do not select a permanent World Project/Bundle, chunk, atlas or streaming format without evidence. If no exact write allocation covers the needed paths, remain read-only and return a bounded allocation proposal to the #486/#162 control plane instead of creating your own writer.
```

## R2. Content catalogue

```text
Oteryn: impl content — programme #486 CONTENT lane. Continue the existing canonical #54/#433 content lineage from LIVE state and build only Reference-first native item, creature, spawn, spell/ability, presentation and relocation definitions that are supported by the accepted Global Reference evidence. Keep provenance/revision binding explicit; test/evidence fixture values must remain non-shipping and must never become Reference defaults. Do not choose a permanent World Bundle format, broaden production activation authority or copy OTS values as proof. Without an exact current allocation, perform read-only gap/readiness analysis and return the smallest legal next allocation.
```

## R3A. Movement

```text
Oteryn: impl movement — programme #486 Reference Movement lane. Reconcile the current Movement issue/allocation, #247 dependency state, resource-limit prerequisites, Client/QA readiness and the accepted Global Reference movement evidence before any write. Prepare/execute only the existing exact Movement lineage for authoritative walk/turn/spatial/collision/visibility behavior; do not absorb Interaction, Combat or Server Seam authority. If production Movement is still blocked, continue only useful read-only Reference fixtures, max/max+1/resource readiness and exact resume planning; do not create a replacement worker or claim parity from structural fixtures.
```

## R3B. Interaction

```text
Oteryn: impl interaction — programme #486 Reference Interaction lane. Fresh-read the current Interaction contracts/implementation and exact allocation. Prepare or implement the smallest Reference-visible use/look/container/pickup/service interaction semantics that are already evidenced and path-disjoint, routing item/value mutations through their canonical owners. Do not invent NPC/quest/item semantics or bypass Durability. If dependencies or ownership are incomplete, stay read-only and return exact gaps/paths/tests needed for a later #162 allocation.
```

## R4. Ability / Spells

```text
Oteryn: impl ability — programme #486 ABILITY-SPELL lane. Use the existing single typed server-authoritative ability/effect engine; do not create a second combat system. Reconcile the accepted Global Reference evidence for each selected representative spell and close only evidenced mana/soul/requirements, cooldown groups, targeting/geometry, range/LOS, damage/heal formulas, conditions/durations, runes/carriers and authoritative timing. Presentation is emitted as semantic events and never owns damage/hit/AoE. Unknown formulas/geometry remain `PARITY_PENDING_EVIDENCE`; structural fixture values stay explicitly non-shipping. Mutate only under the exact live allocation; otherwise return a Reference spell evidence/implementation gap packet.
```

## R5. Combat

```text
Oteryn: sol combat lead — programme #486 Reference Combat lane. Continue from LIVE state and preserve the existing Combat lineage/gates. While Movement/Server Seam/durability prerequisites are incomplete, use read-only preparation to close Reference targeting/attack lifecycle, timing/range, damage/mitigation, death occurrence, XP, corpse, loot and pickup evidence/fixtures plus crash/retry/no-duplication test design. When the exact current Combat allocation and merged prerequisites are proven, resume the same canonical worker and implement through GAME-ABILITY, Interaction, Item and Durability owners; never implement Movement inside Combat. No Evolved balance/death/loot rules and no OTS-derived parity claims.
```

## R6. Creature AI / Spawns

```text
Oteryn: impl ai — programme #486 CREATURE-AI lane. Reconcile the current AI implementation/allocation and Global Reference creature evidence. Prepare/implement only bounded deterministic perception, target selection, chase/flee/retarget, attack/spell-choice, path proposals and spawn lifecycle behavior supported by evidence. AI must propose actions through authoritative Movement/Ability paths and must never own position, combat effects, XP/loot/value or cross-domain transactions. Unknown creature behavior stays explicit. Without a live exact allocation, remain read-only and return the smallest path-disjoint allocation proposal.
```

## R7. Character / Item / Progression

```text
Oteryn: impl domains — programme #486 CHAR-ITEM-PROGRESSION lane. Fresh-read GAME-CHAR, GAME-ITEM, current domain implementation, Reference evidence manifest and exact live allocation. Prepare/execute only evidenced Oteryn Reference vocation/promotion, level/XP/skills, regeneration/stamina/training where in scope, inventory/container/equipment legality, item stats/resists/imbuement semantics, death/loss/blessing/protection and the minimum source/sink rules required by the Reference Playable loop. Explicitly reject leakage from Evolved Rested/death/progression/economy designs unless separately accepted as a Reference difference. Persisted/value work must use its owning Durability boundary; otherwise stay read-only and return exact gaps.
```

## R8. NPC / Quest / Services

```text
Oteryn: impl interaction — programme #486 NPC-QUEST-SERVICES child. Reconcile existing NPC/AI/Interaction/content architecture and active ownership, then target only the minimum Reference service loop: one representative NPC dialogue/service, bounded buy/sell or trade behavior, depot/container service where required, and quest-state model/evidence preparation. Route actions through canonical Interaction/Item/Durability owners; do not invent a second scripting/runtime authority. Current Global/OTS data does not override the immutable Reference cut. If no disjoint write allocation exists, remain read-only and deliver an exact allocation/evidence packet rather than creating a branch.
```

## R9. Client / World presentation / VFX

```text
Oteryn: impl client — programme #486 CLIENT-PRESENTATION lane. Consume protected graphics architecture (#473/#475), protected custom-wgpu foundation evidence (#468) and the live #480 WORLD+VFX prototype instead of restarting renderer selection. Prepare/implement only the exact allocated native-client work for floor/tile/stack/roof rendering, movement interpolation, object/outfit/effect/missile presentation, representative spell/projectile/hit/heal VFX, names/HP/basic HUD and semantically neutral presentation quality. Preserve `RenderSnapshot + PresentationEvents + EnvironmentState` and server authority for damage/hit/AoE/timing. Synthetic/mock producers are allowed only as explicit non-authoritative fixtures until the real server producer is ready. Do not freeze KTX2/DDS, atlas/arrays, streaming or final UI without the relevant evidence gate.
```

## R10. Reference QA / composition

```text
Oteryn: impl qa — programme #486 REFERENCE-QA lane. Build/maintain parity and composition evidence for the accepted Global Reference target without becoming gameplay authority. Progress the journey in dependency order: world entry/presence -> movement/basic interaction -> representative heal+attack -> creature kill -> XP -> corpse -> loot/pickup -> inventory/equipment -> death/respawn/re-entry -> creature AI/spawn -> minimum NPC/depot/trade -> reconnect/restart where required -> native-client presentation. Record each case as PARITY_CONFIRMED / PARITY_PENDING_EVIDENCE / PARITY_CONFLICT / DECLARED_DIFFERENCE / OUT_OF_SCOPE from real evidence. Also prove Reference/Evolved isolation and fail if an undocumented Evolved mechanic appears in Reference. Do not claim the final journey before its real owning runtime seams are protected and exercised.
```

## Reference readiness checkpoint

Use this line only after the #486 coordinator reports all independently executable Reference lanes have been consumed and the real composition prerequisites are available:

```text
Oteryn: work coordinator — programme #486 Reference readiness checkpoint. Freshly reconstruct protected main and all R0-R10 outputs; do not trust lane summaries without GitHub evidence. Prove the immutable target/evidence registry, Reference Playable world/movement/interaction, representative heal+attack, kill->XP->corpse->loot/pickup, inventory/equipment, death/respawn/re-entry, representative AI/spawn, minimum NPC/depot/trade, native client presentation, required reconnect/restart/durability, and Reference/Evolved isolation. Publish a truthful reviewed checkpoint listing every remaining PARITY_PENDING_EVIDENCE/CONFLICT/DECLARED_DIFFERENCE. Do not activate Oteryn Evolved work from this prompt; return the exact owner/control-plane release decision required next.
```

## Later Oteryn Evolved activation — intentionally not executable yet

There is deliberately **no active Evolved implementation launch line in this packet**.

After the Reference readiness checkpoint is protected, a later explicit owner decision must define which Evolved differences are activated and their priority. The control plane should then create a separate Evolved programme/allocation package. Until that happens, any agent encountering an Evolved-only rule while working #486 must exclude it from Reference and record the dependency rather than implementing it.
