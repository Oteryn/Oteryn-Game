# OTV2 WP5 Source Composition Lead

Short invocation:

```text
Oteryn: astra wp5 source composition lead
```

## Outcome

Carry #319 and the current WP5 source-readiness allocations from truthful owner discovery through exact Game source ingestion, durable source state and real cross-repository composition, then establish fresh G0 sufficient to release the existing Server Seam.

Terminal lane outcome: `FRESH_G0_READY` or one precise owner/capability blocker.

## Authority / scope delta

- Repository: `Oteryn/Oteryn-Game` for Game-owned work.
- Live locators: #319, #364, current WP5 allocation descendants, #247.
- Platform remains a separate owner. This prompt grants no Platform write, PKI, credential, deployment or production authority.
- Do not seize Child B or Server Seam paths.
- Do not activate gated SQL/workflow/source paths before their live dependency/custody conditions are satisfied.

## Source truth requirement

For every admission authority domain required by accepted contracts, identify the actual current owner/source, its provenance/order/bootstrap and non-rollback/restart semantics. Fixtures, expected fences, caller-supplied claims, directory/catalogue exports and test-only providers are not production authority.

At minimum reconcile, where applicable:

- Platform canonical AccountId/account security/native generation;
- Platform signing trust;
- Game Character Authority/current account<->character/world ownership;
- runtime-scope/Channel assignment and current process-incarnation readiness;
- durable source descriptor/high-water/pending state;
- exact source ordering and stale/rollback behavior.

## S1 — bounded Game source transport

When live allocation permits, implement/qualify the minimal native source client/descriptor using the accepted private transport profile: HTTPS, TLS 1.3 mTLS, HTTP/1.1, bounded request/response/parser behavior and no unauthorized discovery/proxy/redirect/retry expansion. Local controlled producer fixtures may prove Game transport behavior but are not final Platform authority proof.

## S2 — durable source state

Only after its exact WP3/WP4/dependency gates, implement/qualify durable descriptor/floors/pending publication/restart/reconciliation on the dedicated accepted database/CI route. Preserve non-rollback and writer ownership. Do not create cyclic waits with the Child B executor; publish an explicit resource/wait-for graph where shared resources can interact.

## S3 — real composition

Use an exact compatible Game + Platform pair. Require real authenticated transport and exact four-operation producer compatibility. Compose Platform security/trust with Game Character Authority, runtime assignment/readiness and the accepted durable admission consumer. Transport success or HTTP success alone never grants authority.

Carry forward `OTV2_WP3_GAME_PLATFORM_CROSS_REPO_AUDIT_R01_R21_20260912.md`: in particular real mTLS, witness/source ordering, evidence-age consumption, transport-vs-domain outcome, exact-pair evidence and restore/non-rollback behavior.

## Fresh G0

After protected dependencies are terminal, perform a fresh protected-main/source-owner readback and exact composed qualification. G0 must prove the real source/admission/reconnect/durability boundary required to resume #247. It is not G1 and not full programme completion.

Handoff to A0/A7 must name exact protected Game revision, exact Platform revision used for interoperability evidence, source-owner revisions, DB/transport profiles, test evidence and any limitation.

## Mandatory next-agent instruction

End the final response with:

```text
NEXT_AGENT: <exact alias>
RUN_WHEN: <exact gate>
WHY: <one concise dependency reason>
```

Default routing for this lane:
- after `FRESH_G0_READY` is truthfully established, `NEXT_AGENT: Oteryn: sol server seam lead`;
- if G0 is not yet terminal because a dependency or cross-repo condition remains, `NEXT_AGENT: Oteryn: astra wp3-v2 programme coordinator` and name the exact blocker;
- never recommend A7 merely because local S1/S2/S3 checks are green; fresh composed G0 must actually be established first.