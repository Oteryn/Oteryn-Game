# OTV2 WP3 Writer

Short invocation:

```text
Oteryn: wp3 writer
```

```yaml
prompt_id: OTV2_WP3_WRITER
prompt_version: "1.0"
prompt_mode: WP3_SINGLE_MUTATING_WRITER
repository: Oteryn/Oteryn-Game
lane: WP3_SQLX_DRIVER_ACCOUNTING
short_invocation: "Oteryn: wp3 writer"
```

## Outcome

Continue the single canonical WP3 SQLx-driver-accounting lineage to a stable material candidate that is truthfully `READY_FOR_FINAL_INDEPENDENT_REVIEW` or to one exact evidence-backed blocker.

## Live locators

Resolve live GitHub before acting:

- Issue #351;
- PR #356;
- canonical branch `agent/sqlx-driver-budget-351`;
- current protected `main`;
- current exact WP3 allocation/amendment authority and review/check state.

Historical SHAs and prior chat summaries are locators only.

## Scope delta

You are the only mutating WP3 writer. Preserve the existing branch/PR lineage; do not create a replacement writer, branch or PR. Write only where current protected WP3 authority grants exact ownership.

Current task-specific boundaries include:

- finish active #518 TLS1.2 KX and custom-verifier accounting;
- finish active #501 transcript/hash ownership;
- prove the PSK binder through existing #427 first; #493 remains inactive unless a later explicit valid activation is live;
- preserve one same-ledger owner, reserve before allocation, retain custody until actual backing destruction, and fail underfunded cases before unauthorized allocation;
- preserve TLS bytes/security and owner-free std/no_std behavior.

Do not silently widen into generic message/common-state/vecbuf or any other unallocated surface. Return the minimum exact `SHARED_LEASE_REQUIRED = path :: symbol :: reason` when a real authority boundary is reached.

## Acceptance delta

After complete TLS accounting is genuinely proven, run the already-authorized real SQLx AWS-LC TLS-positive qualification and configured PostgreSQL 17.6 positive/hostile/underfunded/allocation-denial/cancel/error/recovery/restart coverage.

Use focused checks while iterating, then the exact-head qualification selected by current repository policy. Produce whole-diff self-review evidence and a stable exact head for the separate independent HIGH-risk review.

Do not self-approve, mark Ready, enqueue/merge, release WP4 or resume Server Seam. Final integration remains with the active control plane.

Return only:

```text
READY_FOR_FINAL_INDEPENDENT_REVIEW
```

or one precise evidence-backed blocker after all unrelated legal WP3 work is exhausted.
