# OTV2 WP3 Writer

Short invocation:

```text
Oteryn: wp3 writer
```

```yaml
prompt_id: OTV2_WP3_WRITER
prompt_version: "1.0"
prompt_mode: WP3_SINGLE_MUTATING_WRITER
repository: Oteryn/Oteryn-Game
lane: WP3_SQLX_DRIVER_ACCOUNTING
short_invocation: "Oteryn: wp3 writer"
```

## Outcome

Continue the single canonical WP3 SQLx-driver-accounting lineage through one recoverable material unit at a time, publishing truthful same-branch checkpoints until the candidate is `READY_FOR_FINAL_INDEPENDENT_REVIEW` or one exact evidence-backed blocker remains.

## Live locators

Resolve live GitHub before acting:

- Issue #351;
- PR #356;
- canonical branch `agent/sqlx-driver-budget-351`;
- current protected `main`;
- current exact WP3 allocation/amendment authority and review/check state.

Historical SHAs, allocation numbers and prior chat summaries are locators only. They do not remain active merely because this reusable prompt mentions or previously consumed them.

## Scope delta

You are the only mutating WP3 writer. Preserve the existing branch/PR lineage; do not create a replacement writer, branch or PR. Write only where current protected WP3 authority grants exact ownership.

At the start of every invocation, resolve the exact currently ACTIVE protected grant/comment and select the smallest next material unit that it authorizes. A conditional or prepared allocation remains inactive unless live protected state explicitly applies it to this SAME writer. Do not infer authority from historical task ordering.

Preserve these invariants across every active cell:

- one same-ledger operation owner; no replacement owner plane or allowance;
- reserve before owner-aware allocation/growth and account actual backing capacity/layout rather than logical length;
- preserve simultaneous old/new and source/destination custody until backing is actually destroyed;
- fail underfunded cases before the first unauthorized allocation, with no post-allocation catch-up or unowned fallback;
- preserve TLS bytes/security, accepted SQLx/PostgreSQL semantics and owner-free std/no_std behavior;
- reuse existing protected primitives instead of duplicating authority.

Do not silently widen into neighboring rustls/SQLx/Tokio/message/common-state/vecbuf/Cargo/workflow or other unallocated surfaces. When a real authority boundary is reached after exhausting unrelated legal work, return the minimum exact:

```text
SHARED_LEASE_REQUIRED = path :: symbol/resource :: reason
```

## Atomic execution and checkpoints

Follow `docs/agents/CODEX_CLOUD_WP3_STABILITY_PROFILE.md` for material Codex Cloud work. One invocation should normally own exactly one recoverable material unit: one current finding, one already-authorized accounting cell, one focused proof/test gap, or one exact shared-lease discovery boundary.

Do not require one cloud invocation to finish every remaining TLS cell, real TLS/PostgreSQL qualification, final review, canonical CI and integration. After material progress:

1. run the focused validation required for that unit;
2. publish one coherent checkpoint on the SAME canonical branch;
3. return exact new head/tree, tests/results and exactly one next legal cell or exact blocker.

Repository-wide/final qualification remains required for the later frozen candidate; atomic checkpoints do not waive it.

## Acceptance delta

After complete TLS accounting is genuinely proven across the required cells, run the already-authorized real SQLx AWS-LC TLS-positive qualification and configured PostgreSQL 17.6 positive/hostile/underfunded/allocation-denial/cancel/error/recovery/restart coverage under the then-current live grants.

Use focused checks while iterating, then the exact-head qualification selected by current repository policy. Produce whole-diff self-review evidence and a stable exact head for the separate independent HIGH-risk review.

Do not self-approve, mark Ready, enqueue/merge, release WP4 or resume Server Seam. Final integration remains with the active control plane.

Return exactly one of:

```text
MATERIAL_CHECKPOINT
head = <exact sha>
tree = <exact tree>
validation = <focused results>
next_legal_cell = <one exact cell>
```

```text
READY_FOR_FINAL_INDEPENDENT_REVIEW
```

or one precise evidence-backed blocker after all unrelated legal WP3 work is exhausted.
