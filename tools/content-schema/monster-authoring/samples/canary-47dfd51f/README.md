# Canary test batch — 10 monsters

Source: `opentibiabr/canary@47dfd51f45280a59a1d3e50ba7edd573d7234446`. Generated by
`../../canary_batch.py` (requires `lupa`); `sources.json` binds every consumed file by Git blob.
Classification: **OTS_HYPOTHESIS_ONLY** source evidence. The `canary:` keys are provisional
source-scoped keys, not native Oteryn identities, and are never promoted to Game truth by a Wiki comparison.

Each directory holds `monster.json`, `dependencies.json`, `catalog.json` (declared, unproven
external references and assets) and `manifest.json` (per-field source disposition).

## Coverage

| Monster | Exercises |
|---|---|
| rat | baseline melee, 2 loot entries, quest event script |
| cyclops | shared summon/convince mana cost, resistances, voices, 13 loot entries |
| orc_spearman | distance keeping (`target_distance_tiles=4`), projectile physical attack |
| scorpion | melee plus poison damage-over-time condition |
| orc_shaman | caster, radius area, self-heal defense, summons |
| necromancer | three summon kinds, poison melee, death/earth spells |
| fire_elemental | `firefield` create-item effect, no loot |
| dragon | radius and wave (length/spread) areas, heal, 21 loot entries, two event scripts |
| ghost | life drain, undead race |
| demodras | boss (`reward_boss`), summons a batch member, all area kinds |

Not exercised by this batch: speed/outfit/invisible/drunk spells, damage-type immunities,
reflection/healing-from-damage, bosstiary, familiar, nested container loot, `unique` loot,
item-look outfits, non-zero light and registered spell scripts.

## Result

- 10/10 bundles pass `validate_monster.py` (structure, semantics, declared-reference closure).
- 10/10 import manifests resolve every declared row (`validate_monster.py ... --manifest`) after
  owner decisions D5–D7 in the architecture document. The first run was blocked in all 10 by:
  `pass_through` without a Canary counterpart (D5), race -> death residue (D7; blood x7, venom
  x1, none for undead and fire) and 3 quest task event rows in rat/dragon (D6).
- "Resolved" covers only the declared manifest rows. It does not prove source-census
  completeness, canonical Item/asset admission (all `canary:` references are declarations),
  runtime behaviour or Global parity.
- Every loot item name resolved to exactly one Canary item id; the rule applied is recorded in
  `sources.json`. Canary has 3,258 ambiguous item names overall, so larger batches will hit them.
- Values that follow a documented source rule rather than a literal field are marked in the
  manifest `resolution` text: melee chance normalized to 100, Bestiary difficulty/occurrence
  derived from numeric codes, engine defaults for absent flags, `CONST_ME_POFF` for area spells
  without an effect, corpse weight 0 when items.xml has none, and config-dependent element
  clipping.

## Current Wiki comparison

The owner replaced the historical reference date with the current state. Both Wiki APIs
were read on 2026-09-26 without a date selector. The returned latest revisions can have
older edit timestamps; revision IDs, digests and acquisition/server timestamps bind the facts.

- `wiki-current-fandom.json`, produced by `../../wiki_compare.py`: **229 MATCH,
  12 DIFF, 8 WIKI_UNKNOWN, 10 NOT_COMPARABLE** over 259 rows.
- `wiki-current-comparison.json`, produced by `../../wiki_current_comparison.py`:
  **173 MATCH, 13 CONFLICT, 20 UNKNOWN, 19 NOT_COMPARABLE** over 225 rows.
- `WIKI_CURRENT_COMPARISON.md` explains the differing observations and limits.

The Wiki sources remain separate observations. Different totals also reflect different
field coverage; they are not combined into a single score. Raw speed units and loot
probabilities are not qualified. Bundles preserve pinned Canary values.

```text
python wiki_compare.py --canary <pinned checkout> --cache <local Fandom cache directory> --refresh
python wiki_current_comparison.py --cache <local BR cache.json> --refresh
```

Without `--refresh`, each collector reproduces its local current-source cache. Raw Wiki
prose stays in local scratch and is not committed. Historical caches are rejected.
