# Canary test batch 2 — 10 monsters

Source: `opentibiabr/canary@47dfd51f45280a59a1d3e50ba7edd573d7234446`, generated by
`../../canary_batch.py --batch canary-47dfd51f-batch-2`. Same classification and key rules as
batch 1 (`../canary-47dfd51f/README.md`). Chosen to exercise what batch 1 did not.

## Coverage

| Monster | Exercises |
|---|---|
| morshabaal | nemesis boss (bosstiary), paralyze clamped to -1000, haste, drunk, invisible, outfit -> creature and -> item, inert `strength` visuals |
| dworc_voodoomaster | the same spell kinds on an ordinary monster |
| wisp | non-zero light, haste, invisible |
| the_enraged_thorn_knight | archfoe boss, heals-from-damage, `unique` loot, addon outfit, encounter events |
| sand_vortex | item-look outfit (`lookTypeEx`), damage reflection, drunk |
| knight_familiar | familiar profile, registered spell scripts, no corpse, empty voices, missing lookType |
| blood_hand | bleeding damage-over-time, `effect` visual-only spell |
| mad_mage | bane boss, `unique` loot |
| crazed_summer_rearguard | damage reflection on an ordinary monster |
| war_golem | registered spell scripts, light, outfit transform |

Not present in the 1,001 of 1,656 otservbr monster files inspected locally (the rest were not
fetched): combat (damage-type) immunities and nested `child` container loot, so neither can be
exercised from this source.

## Result

- 10/10 bundles pass `validate_monster.py`; 7/10 manifests resolve every declared row.
- Blocked, as intended:
  - `knight_familiar`: registered spells `sudden death rune` and `ice strike` need a native
    Ability resolution; the monster file has no `lookType` because familiar looks are chosen per
    player in `data/XML/familiars.xml`.
  - `war_golem`: registered spells `war golem electrify` and `war golem skill reducer`.
  - `the_enraged_thorn_knight`: `HealthForgotten` doubles damage taken unless a Possessed Tree is
    near (D9: Encounter mechanic); addon outfit attachments are not mapped.
- Schema change from this batch: bosstiary points are per stage (D8).
- Familiar profile is not in the monster file: vocation and mana come from
  `data/scripts/spells/familiar/`, duration from `CreateFamiliarSpell` with config default
  `familiarTime=30` (900000 ms). Config-dependent values are recorded in the manifest.
- Creature events are classified only after reading their script (`EVENTS` in the converter);
  unknown events stay unresolved.

## Findings for the schema owner

- Effects carry no explicit recipient. The engine derives it from the spell's aggressive flag and
  geometry (haste, invisible and outfit-on-self are non-aggressive and hit the caster). The
  authoring model currently leaves this implicit.
- Damage reflection, like element resistance, is clipped by server configuration
  (`MAX_DAMAGE_REFLECTION`); values are stored as authored.
